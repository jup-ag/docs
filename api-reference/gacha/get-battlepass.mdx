---
openapi: /openapi-spec/gacha/gacha.yaml get /rewards/battlepass
title: "Get Battlepass"
description: "A wallet's battlepass progress and per-tier claim state"
llmsDescription: "GET /gacha/v2/rewards/battlepass returns a wallet's battlepass progress: USD spent on the campaign's eligible pack types and each tier's threshold, reward and claim state (locked, claimable, delivering, delivered). Only spend on eligiblePackTypes counts, so a wallet can have leaderboard volume and no battlepass progress. Pass wallet (base58)."
hidden: true
---
