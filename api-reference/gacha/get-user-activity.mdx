---
openapi: /openapi-spec/gacha/gacha.yaml get /users/{wallet}/activity
title: "Get User Activity"
description: "A wallet's pull history across every platform"
llmsDescription: "GET /gacha/v2/users/{wallet}/activity returns a wallet's pull history across every platform, newest first, keyset paged: pass limit (max 100, default 25) and the previous response's nextCursor to continue. Each entry names the machine, pack type, item won, its value and the time of the pull."
hidden: true
---
